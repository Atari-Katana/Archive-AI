# Archive-AI Production Environment Variables
# Copy this file to .env and configure for your deployment

# === Security ===
REDIS_PASSWORD=changeme_strong_password_here

# === Service Ports ===
BRAIN_PORT=8080

# === Memory Archival ===
ARCHIVE_DAYS=30          # Archive memories older than N days
ARCHIVE_KEEP=1000        # Keep N most recent memories in Redis
ARCHIVE_HOUR=3           # Daily archival hour (0-23)
ARCHIVE_MINUTE=0         # Daily archival minute (0-59)

# === Voice Configuration ===
WHISPER_MODEL=base       # Options: tiny, base, small, medium, large
WHISPER_DEVICE=cpu       # Options: cpu, cuda
TTS_DEVICE=cpu           # Options: cpu, cuda

# === Library Drop Location ===
LIBRARY_DROP=~/ArchiveAI/Library-Drop

# === Logging ===
LOG_LEVEL=INFO           # Options: DEBUG, INFO, WARNING, ERROR

# === GPU Configuration ===
CUDA_VISIBLE_DEVICES=0   # GPU device ID
GPU_MEMORY_UTILIZATION=0.22  # vLLM GPU memory fraction (0.0-1.0)

# Production Notes:
# 1. Generate strong REDIS_PASSWORD: openssl rand -base64 32
# 2. Ensure LIBRARY_DROP directory exists and is writable
# 3. Set LOG_LEVEL=WARNING in production to reduce noise
# 4. Monitor archive directory size: du -sh data/archive
# 5. Backup .env file securely (contains passwords)
